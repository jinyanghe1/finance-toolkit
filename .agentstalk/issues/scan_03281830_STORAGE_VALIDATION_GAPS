# 扫描发现的问题

## 时间: 03281830
## Issue ID: scan_03281830_STORAGE_VALIDATION_GAPS

### 问题: 存储层缺少校验，且存在字段回读缺口
**严重程度**: 中
**文件**:
- models.py:120-144
- company_analyzer.py:72-76
- storage/company_db.py:47-58
- storage/company_db.py:86-120

**描述**:
- `CompanyProfile.from_dict()` 对输入结构和枚举值缺少保护
- `update_market_data()` 使用 `setattr()` 写入任意同名字段，缺少类型/范围校验
- `load_profile()` / `load_metrics()` 对 JSON 结构默认信任
- `FinancialMetrics.roic` 已定义，但 `load_metrics()` 未将其从 JSON 读回

**影响**:
- 异常或脏数据容易在加载时抛错
- 数值字段可能被写入错误类型
- `ROIC` 等字段存在“写得出、读不回”的语义缺口

**建议修复**:
- 为反序列化和更新入口增加最小校验
- 补齐 `ROIC` 的读回映射
- 视并发需求引入更稳健的写入策略

