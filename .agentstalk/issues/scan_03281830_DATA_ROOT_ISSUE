# 扫描发现的问题

## 时间: 03281830
## Issue ID: scan_03281830_DATA_ROOT_ISSUE

### 问题: 数据根目录硬编码
**严重程度**: 高
**文件**:
- storage/company_db.py:15-23

**描述**:
`CompanyDB` 默认将数据写入 `/root/.openclaw/workspace/data/finance`。
该路径与 README 中声明的 `/data/finance/company/{code}/` 不一致，且依赖特定环境。

**影响**:
- 非目标机器上可移植性差
- 在权限不足或目录不存在的环境中会直接失效
- 使 demo / 主入口即使修复导入后，仍可能因环境路径问题出现额外失败

**建议修复**:
- 改为通过环境变量或构造参数注入数据根目录
- 提供合理默认值（例如用户目录下的应用数据目录）

